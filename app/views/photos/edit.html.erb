<div class="container">
  <div class="row">
    <div class="top-message col-md-offset-3 col-md-6" >
      <div class="field_with_errors" style="color: red;">
        <% if @photo.errors.any? %>
          <h2><%= @photo.errors.count %>errors prohibited this photo from being saved:</h2>
          <ul>
            <% @photo.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
          </ul>
        <% end %>
      </div>

      <h1>Editing Photo</h1>
        <%= form_for(@photo) do |f| %>
          <div class="row">
            <label>Title</label>
            <%= f.text_field :title, class: "col-xs-12" %>
          </div>
          <div class="row">
            <label>body</label>
            <%= f.text_area :body, class: "col-xs-12" %>
          </div><br>
          <div class="row">
            <%= f.attachment_field :photo_image %>
          </div><br>
          <div class="row">
            <p><%= f.submit 'Update Photo', class: "btn btn-primary col-xs-12" %></p>
          </div>
        <% end %>
        <div class="link" style="color: #666;">
          <%= link_to "Show", photo_path(@photo.id), class: "text-muted"%>
          <span> |</span>
          <%= link_to "Back", "/photos", class: "text-muted" %>
        </div>
    </div>
  </div>
</div>
